version: "3.9"
services:
  mqdockerup:
    image: micrib/mqdockerup
    env_file:
      - .env
    volumes:
      - /var/run/docker.sock:/var/run/docker.sock
    environment:
      - MQTT_CONNECTIONURI=mqtt://broker:1883
